services:
  - type: web
    name: trading-alerts
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py --live
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
